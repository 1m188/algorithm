/*
标题：魔方状态

二阶魔方就是只有2层的魔方，只由8个小块组成。
如图p1.png所示。

小明很淘气，他只喜欢3种颜色，所有把家里的二阶魔方重新涂了颜色，如下：

前面：橙色
右面：绿色
上面：黄色
左面：绿色
下面：橙色
后面：黄色

请你计算一下，这样的魔方被打乱后，一共有多少种不同的状态。

如果两个状态经过魔方的整体旋转后，各个面的颜色都一致，则认为是同一状态。

请提交表示状态数的整数，不要填写任何多余内容或说明文字。
*/

/*
不知道该怎么做。。。看了网上的几个解答，答案都不一样。。。
我用的是其中一种方法，魔方总共有8块，
然后这8块其中有4块一样，另外2块一样，另外又2块一样，然后就是判断
1 1 1 1 2 2 3 3 的全排列，且这8块的位置不能互换，
换了就是另一种情况了，然后对称的情况就直接/2，这样用dfs判断
一下最后得出的答案和我参考的那篇博客都不一样。。。
其他的比如说有用Burnside引理判断的，看不懂。。。。
还有什么空间搜索+判重什么的。。。。感觉很麻烦。。。
*/

#include <cstdio>

/*
 * 正确解法：
 *   二阶魔方 8 个角块，配色为：前=橙、后=黄、左=绿、右=绿、上=黄、下=橙。
 *   各角块的三个可见面颜色（按该位置对应的面方向）：
 *     FRT:橙-绿-黄    FLT:橙-绿-黄    FRB:橙-绿-橙    FLB:橙-绿-橙
 *     KRT:黄-绿-黄    KLT:黄-绿-黄    KRB:黄-绿-橙    KLB:黄-绿-橙
 *
 *   由此角块分为三类：
 *     类型1 (橙-绿-黄,三色各异): 4 个 -> 只能放入 FRT/FLT/KRB/KLB（共4个位置）
 *     类型2 (橙-绿-橙,两橙一绿): 2 个 -> 只能放入 FRB/FLB
 *     类型3 (黄-绿-黄,两黄一绿): 2 个 -> 只能放入 KRT/KLT
 *
 *   每个块在其兼容位置上仅有一种有效朝向（各面颜色必须对齐）。
 *   8 个位置两两形成镜像对，同对内的两个位置交换同类块视觉不变。
 *   因此类型1的4个块分配给前组（FRT+FLT）和后组（KRB+KLB）：
 *     C(4,2) = 6 种选择（哪2个去前组）。
 *   类型2、类型3各只有1对位置，同类块交换无区别，各1种。
 *   整体旋转无对称性（橙≠黄≠绿），最终答案 = 6。
 */

int main() {
    printf("6\n");
    return 0;
}
