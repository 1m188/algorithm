/*

标题：分机号

X老板脾气古怪，他们公司的电话分机号都是3位数，老板规定，所有号码必须是降序排列，且不能有重复的数位。比如：

751,520,321 都满足要求，而，
766,918,201 就不符合要求。

现在请你计算一下，按照这样的规定，一共有多少个可用的3位分机号码？

请直接提交该数字，不要填写任何多余的内容。

*/

#include <stdio.h>

// 解法：从 0~9 共10个数字中任选3个不同数字，按降序排列唯一确定一个合法号码
// 总数 = C(10,3) = 120

int main() {
    // 方法一：组合数学公式 C(10,3) = 10*9*8 / 6 = 120
    int result = 10 * 9 * 8 / 6;
    printf("%d\n", result);

    // 方法二（验证）：枚举所有三位数，检查是否满足降序且无重复条件
    // 如需验证可取消下方注释运行
    /*
    int count = 0;
    for (int num = 100; num <= 999; num++) {
        int a = num / 100;        // 百位
        int b = num / 10 % 10;    // 十位
        int c = num % 10;         // 个位
        if (a > b && b > c) {     // 严格降序，同时保证了无重复
            count++;
        }
    }
    printf("枚举验证: %d\n", count);
    */

    return 0;
}
