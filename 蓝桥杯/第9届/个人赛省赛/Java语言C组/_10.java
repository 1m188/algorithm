/*

标题：耐摔指数

x星球的居民脾气不太好，但好在他们生气的时候唯一的异常举动是：摔手机。
各大厂商也就纷纷推出各种耐摔型手机。x星球的质监局规定了手机必须经过耐摔测试，并且评定出一个耐摔指数来，之后才允许上市流通。

x星球有很多高耸入云的高塔，刚好可以用来做耐摔测试。塔的每一层高度都是一样的，与地球上稍有不同的是，他们的第一层不是地面，而是相当于我们的2楼。

如果手机从第7层扔下去没摔坏，但第8层摔坏了，则手机耐摔指数=7。
特别地，如果手机从第1层扔下去就坏了，则耐摔指数=0。
如果到了塔的最高层第n层扔没摔坏，则耐摔指数=n

为了减少测试次数，从每个厂家抽样3部手机参加测试。

如果已知了测试塔的高度，并且采用最佳策略，在最坏的运气下最多需要测试多少次才能确定手机的耐摔指数呢？

输入数据，一个整数n（3<n<10000）,表示测试塔的高度。
输出一个整数，表示最多测试多少次。

例如：
输入：
3

程序应该输出：
2

解释：
手机a从2楼扔下去，坏了，就把b手机从1楼扔；否则a手机继续3层扔下

再例如：
输入：
7

程序应该输出：
3

解释：
a手机从4层扔，坏了，则下面有3层，b,c 两部手机2次足可以测出指数；
若是没坏，手机充足，上面5,6,7 三层2次也容易测出。

资源约定：
峰值内存消耗（含虚拟机） < 256M
CPU消耗  < 1000ms


请严格按要求输出，不要画蛇添足地打印类似：“请您输入...” 的多余内容。

所有代码放在同一个源文件中，调试通过后，拷贝提交该源码。
不要使用package语句。不要使用jdk1.7及以上版本的特性。
主类的名字必须是：Main，否则按无效代码处理。



---------------------------------
笨笨有话说：
我觉得3个手机太难了，要是2个手机还可以考虑一下。

歪歪有话说：
想什么呢，你！要是1部手机还用你编程啊？那样的话只好从下往上一层一层测。



*/

import java.util.*;

public class _10 {

    /**
     * 耐摔指数（K 部手机 / 鸡蛋掉落问题）
     *
     * 给定 n 层楼和 3 部手机，采用最优策略，求在最坏运气下确定耐摔指数
     * 所需的最少测试次数。
     *
     * 核心思路：
     * 定义 g(k, m) = 拥有 k 部手机和 m 次测试机会时，最多能确定多少层楼。
     * 递推公式：
     * g(k, m) = g(k-1, m-1) + 1 + g(k, m-1)
     * 含义：首次在 x 层测试，
     * - 若摔坏：用 k-1 部手机在 m-1 次内测下方 g(k-1, m-1) 层
     * - 若没坏：用 k 部手机在 m-1 次内测上方 g(k, m-1) 层
     * - 加上当前测试的这一层，合计 g(k-1, m-1) + 1 + g(k, m-1)
     *
     * 已知：
     * g(1, m) = m （1 部手机只能逐层测）
     * g(2, m) = m(m+1)/2 （闭式解）
     * g(k, 0) = 0
     *
     * 问题转化为求最小的 m 使得 g(3, m) ≥ n。
     * 可利用 g(2, m) 的闭式解简化递推：
     * g(3, m) = g(3, m-1) + 1 + g(2, m-1)
     * = g(3, m-1) + 1 + (m-1)·m / 2
     *
     * 时间复杂度：O(m)，其中 m 约为 O(n^{1/3})，n=10000 时 m ≈ 40。
     * 空间复杂度：O(1)，仅需常数变量。
     */

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        sc.close();

        /*
         * g3 代表 g(3, m)：当前尝试次数 m 下可测的最大楼层数。
         * m 从 0 开始逐一递增，直至 g3 ≥ n。
         */
        int m = 0;
        int g3 = 0; // g(3, 0) = 0

        while (g3 < n) {
            m++;
            /*
             * g(3, m) = g(3, m-1) + 1 + g(2, m-1)
             * g(2, m-1) = (m-1)·m / 2
             */
            g3 = g3 + 1 + (m - 1) * m / 2;
        }

        System.out.println(m);
    }
}
