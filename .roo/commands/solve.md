---
description: 解决算法题目
---

- 解决文件中所描述的算法题目，不要修改或者删除已有的重要内容如注释等，仅仅输出代码和必要注释即可。
- 写完代码后进行测试，覆盖主流用例和边缘用例。
- 在做完所有工作之后删除所有测试代码和编译产物等，仅仅保留纯净的解决方案代码文件即可。
- 代码需要格式化，部分语言的格式化要求可以参见工作区根目录的相关配置文件。[C/C++](../../.clang-format)；Python遵循black默认格式化规则；golang、rust、C#使用默认格式化规则。如果工作区没有相关语言的格式化规则则使用默认的格式化规则。
