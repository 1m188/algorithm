#include <iostream>
using namespace std;

int main() {

    ios::sync_with_stdio(false);
    cin.tie(0);

    int A, B, C, D, E, F;
    cin >> A >> B >> C >> D >> E >> F;

    cout << A + B + C + D + E + F;

    return 0;
}
// 64 位输出请用 printf("%lld")